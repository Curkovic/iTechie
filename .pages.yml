media:
  input: .
  output: /

content:
  - name: blog
    label: Blog
    type: collection
    path: blog
    format: raw
    filename: "{{slug}}.html"
    fields:
      - {name: title, type: string}
      - {name: body, type: rich-text}
